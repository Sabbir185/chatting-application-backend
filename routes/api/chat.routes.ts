import {Router} from 'express';
const chatRoutes = Router();

import {

} from '../../controllers/user.controller'


chatRoutes.post('/', );
chatRoutes.get('/', );
chatRoutes.post('/group', );
chatRoutes.post('/rename', );
chatRoutes.post('/groupremove', );
chatRoutes.post('/groupadd', );



export default chatRoutes;

